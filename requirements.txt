# Dependencias reales del proyecto (sin el lastre del pip freeze anterior:
# customtkinter, pypdfium2, pyinstaller y SQLAlchemy venían de funciones que
# ya se descartaron y solo alargaban la instalación en CI).
loguru==0.7.3
playwright==1.60.0
psutil==7.2.2
pydantic==2.13.4
python-dotenv==1.2.2

# Base de datos de zonas horarias: Windows no trae una y zoneinfo la necesita.
tzdata>=2024.1
